apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
    - { name: prometheus, port: {{ .Values.service.prometheusPort }}, targetPort: prometheus }
